
  <% if @visiblenode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visiblenode.errors.count, "error") %> prohibited this visiblenode from being saved:</h2>

      <ul>
      <% @visiblenode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="Label"> node_id </div><br />
    <%= number_field_tag :node_id %>
  </div>
  <div class="field">
    <div class="Label"> player_id </div><br />
    <%= number_field_tag :player_id %>
  </div>
  <div class="actions">
    <%= submit_tag :Submit %>
  </div>
