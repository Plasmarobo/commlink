
  <% if @npc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@npc.errors.count, "error") %> prohibited this npc from being saved:</h2>

      <ul>
      <% @npc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="Label"> name </div><br />
    <%= text_field_tag :name %>
  </div>
  <div class="field">
    <div class="Label"> desc </div><br />
    <%= text_field_tag :desc %>
  </div>
  <div class="field">
    <div class="Label"> secret </div><br />
    <%= text_field_tag :secret %>
  </div>
  <div class="actions">
    <%= submit_tag :Submit %>
  </div>
