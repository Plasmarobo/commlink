<div class="FormContainer">
	<% @skillset.item_names.each do |c, d| %>
		<div class="EditCell">
			<div class="EditCellLabel"><%= d%></div>
			<%= text_field_tag ("skill_" + c), @skillset.send(c), :class => "EditCellInput"%>
		</div>
	<% end %>
</div>
